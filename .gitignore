# External repos — now managed as git submodules
# (see .gitmodules)

# ROS2 build artifacts
build/
install/
log/
logs/

# ArduPilot runtime files
eeprom.bin
mav.parm
mav.tlog
mav.tlog.raw
terrain/
mav_0_1.parm